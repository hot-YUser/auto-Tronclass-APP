# auto-Tronclass-APP 第二輪對抗審查

> 審查日期：2026-08-10
> 審查基準：`ce36e17c750d50ff072c25d2188143be0c9fe560`
> v2：`C:\Users\YUser\Documents\auto-Tronclass-APP`
> v1 參考：`C:\Users\YUser\Documents\ARTT-clean-git`
> 審查模式：唯讀；不修改程式碼、設定、文件或 Git 狀態
> 審查原則：未交付的反駁不計為確認或否定；每一項必須標為「已確認」、「已反駁」、「條件性」或「未驗證」。

## 一、總結裁決

目前版本**尚未完善，不建議解除發版凍結**。

另一個 AI 所完成的 hardening 與 CI 綠燈均有實質價值，也不是偽造結果；但現有測試沒有覆蓋數個正式跨模組契約。第二輪審查確認至少四項可直接影響活動顯示、提交結果或簽到真實性的 P1 缺陷，因此不能以「CI 全綠」推導為「產品已完整修復」。

本輪最重要的結論如下：

1. Vote／Homework 的合法空 `instance_id` 會被正式 UI 契約解析器拒絕。
2. HTTP mutation 可能把 SSO 登入頁、非 JSON 200 或明確業務拒絕判為成功。
3. 多帳號部分準備失敗時，失敗帳號會從 UI 消失，最後可能顯示成全部提交成功。
4. 數字點名的寬鬆文字判定仍可能把拒絕或未知 2xx 回應認定為簽到成功。
5. 審查矩陣本身仍保留真機、正式簽章、完整秘密生命週期與 A–E 反駁等未驗證項目。

## 二、審查時的基準狀態

- `HEAD`：`ce36e17c750d50ff072c25d2188143be0c9fe560`
- 分支：`main`
- 遠端：`origin/main` 與審查時 HEAD 一致。
- tracked working tree：乾淨。
- 既有未追蹤文件：
  - `AI交接紀錄-2026-08-09.md`
  - `第一輪審查.md`
- 最新精確對應 HEAD 的 CI：<https://github.com/hot-YUser/auto-Tronclass-APP/actions/runs/31326369003>

該 CI 實際結果：

- Rust：117 passed、0 failed、10 ignored。
- Windows UI build：成功，0 warning、0 error。
- `ProtocolContract.Check`：成功。
- `DeviceKey.Check`：成功。
- Android native ABI build：成功。
- Android MAUI build：成功，0 warning、0 error。

以上證明目前提交可以通過既有 CI，但不代表被忽略的 live tests、正式發版腳本、簽章產物、真機服務生命週期及負向業務回應均已驗證。

## 三、P1：已確認的發版阻斷

### P1-1：Vote／Homework 的 `QuizPrepared` 會被 UI 丟棄

**狀態：已確認。**

Rust 為 Vote、Homework 合法地產生空的 `instance_id`：

- `core/src/answer.rs:95-133`

C# `QuizPreparedContract` 卻無條件拒絕任何空 `instance_id`：

- `ui/Interop/QuizPreparedContract.cs:55-57`

而 Vote 的提交路徑本身不需要 instance：

- `core/src/monitor.rs:2008-2012`

因此實際流程會是：core 成功準備活動 → 發出 `QuizPrepared` → UI 契約解析失敗 → 活動不顯示、無法由 UI 提交。

現有契約 fixture 只覆蓋具有非空 instance 的 Exam，沒有跨 activity source 測試。第一輪矩陣對 P1-4 的「已修復」結論因此並不完整。

**最低驗收條件：**

- 明確定義各 activity source 的 instance 語義，不能使用全域非空限制。
- Exam、Questionnaire、Vote、Courseware、Homework、Classroom 均須具備 Rust → JSON → C# parser 的共享 fixture。
- fixture 必須同時覆蓋合法空值、合法非空值及非法缺欄位。

### P1-2：Mutation 可能把登入頁或業務拒絕判為成功

**狀態：已確認。**

`reqwest::Client` 預設跟隨重新導向，而共用 HTTP helper 只檢查最終狀態：

- `core/src/http.rs:10-29`

`mutation_checked` 對非 JSON body 不回傳錯誤。所以下列回應鏈會被判為成功：

1. POST 提交。
2. 伺服器回傳 302 至 SSO／login。
3. client 跟隨至 200 HTML 登入頁。
4. 最終狀態為 2xx，HTML 又不是可辨識的 JSON 業務錯誤。
5. helper 回傳成功，monitor 發出 `QuizSubmitted`。

相關提交路徑：

- `core/src/monitor.rs:1998-2024`
- `core/src/monitor.rs:2194-2208`

Exam 還有另一個缺口：

- `core/src/answer.rs:436-448`

若回應為 `{"success":false,"submission_id":123}`，只要能抽出 submission id，仍可能被認為提交成功。

更正答案路徑則只使用 HTTP status gate：

- `core/src/answer.rs:492-496`

因此 2xx 業務拒絕也可能被報為更正完成。

**最低驗收條件：**

- Mutation 對 login／SSO redirect 必須 fail-closed。
- 200 HTML、空 body、schema 不符、`success:false`、非零 `error_code` 均需有負向測試。
- 每一種活動只能在明確且 source-specific 的成功條件成立後發出 `QuizSubmitted`。

### P1-3：多帳號部分失敗會被隱藏並顯示全部完成

**狀態：已確認。**

core 的 `QuizPrepared` payload 只包含 Ready、Submitting、Submitted attempts：

- `core/src/monitor.rs:2064-2072`

Failed／Gone attempt 只另外送出 Error，沒有保留在該次 prepared snapshot。

UI 收到 prepared event 後，會移除不在此次 payload 的所有帳號：

- `ui/State/AppState.cs:348-350`

收到單一帳號的 submitted event 後，又使用目前剩餘帳號的 `All(a => a.Submitted)` 判定整份活動完成：

- `ui/State/AppState.cs:399-410`

可重現的邏輯情境：

1. 帳號 A 準備失敗。
2. 帳號 B 成功準備。
3. prepared snapshot 只含 B，A 被 UI 刪除。
4. B 提交成功。
5. UI 只看剩餘的 B，顯示整份活動已全部提交。

這會隱藏真實失敗帳號並向使用者報告錯誤結果。

**最低驗收條件：**

- 建立「A 準備失敗、B 成功」的 core event + UI state integration test。
- 失敗帳號必須保留可見狀態，不得因 snapshot 缺席而消失。
- 活動完成判定必須以預期參與帳號集合為基準，而不是只看目前仍存在的 UI collection。

### P1-4：數字點名仍可能誤判成功

**狀態：已確認。**

`parse_number_response` 對未辨識的 2xx body 預設回傳 Success：

- `core/src/rollcall.rs:202-232`

目前沒有完整檢查 `error_code`，成功 marker 又是裸 substring；例如 `unsuccessful` 可能命中 `success`、`not ok` 可能命中 `ok`。

提交及 brute-force 路徑會接受這個 Success：

- `core/src/rollcall.rs:295-314`
- `core/src/rollcall.rs:335-371`

monitor 接著發出已簽到事件：

- `core/src/monitor.rs:1023-1033`

若沒有後續 roster recheck，使用者可能看到假成功。

**最低驗收條件：**

- 未辨識回應預設必須是錯誤或 Unknown，不得是 Success。
- 使用結構化 JSON schema 與完整 token，而不是裸 substring。
- 覆蓋 `unsuccessful`、`not ok`、`error_code`、login JSON、空 body、HTML body。
- 可行時，以 roster recheck 作為成功確認，而不是只信 submission response。

## 四、P2：已確認的重要缺口

### P2-1：Reply envelope 為 fail-open

**狀態：已確認。**

`OkReply` 只有在 `ok` 明確存在且為 `false` 時才回傳失敗；缺欄位、null、錯誤型別或 `{}` 都被視為成功：

- `ui/State/AppState.cs:544-545`

Native boot 也只有遇到明確的 `ok:false` 才丟出錯誤：

- `ui/Interop/NativeCore.cs:56-64`

這可能使設定、監控、hold／discard 等命令在 malformed reply 下被 UI 標示為成功。

### P2-2：Malformed native event 可能逃逸至 UI dispatcher

**狀態：已確認。**

AppState subscriber 將 Route 投遞至 MainThread，但 Route 本身沒有總體 try/catch：

- `ui/State/AppState.cs:17-20`
- `ui/State/AppState.cs:104-107`

Providers、Accounts、rollcall 等路徑直接對未驗證 JSON 呼叫 `EnumerateArray()`，Settings 整數直接 `GetInt32()`：

- `ui/State/AppState.cs:122-127`
- `ui/State/AppState.cs:202-208`
- `ui/State/AppState.cs:553-554`

malformed event 或數字溢位可能在 UI dispatcher 逃逸。現有 `ProtocolContract.Check` 只覆蓋 QuizPrepared，並沒有 State／Accounts／Settings 的 malformed fixtures。

### P2-3：舊版 Windows 資料遷移不是交易式

**狀態：已確認。**

`DataPaths`：

- 使用新 vault 是否存在作為遷移完成信號。
- 逐檔複製 vault、config、key。
- 捕捉所有錯誤並當成新安裝繼續。

證據：

- `ui/State/DataPaths.cs:56-70`

若 vault 已複製但 config／key 尚未完成時崩潰或磁碟滿，下次啟動會看到新 vault 並跳過遷移，可能造成 key mismatch 或帳號消失。

### P2-4：產品層沒有真正可用的 boot retry

**狀態：已確認。**

NativeCore API 可再次 boot，但 AppShell 只呼叫一次，之後永久設 `_booted = true`：

- `ui/AppShell.cs:37-42`

AppState 捕捉失敗並停用監控：

- `ui/State/AppState.cs:23-38`

UI 沒有再次呼叫 `BootAsync` 的操作入口，同一 process 仍需要重啟。因此審查矩陣對「Boot failure retry 已修復」的產品層結論過度樂觀。

### P2-5：多帳號 LLM reasoning stream 會混流

**狀態：已確認。**

Rust `ReasoningChunk` 只有 activity token 與 subject id，沒有 account id：

- `core/src/llm.rs:210-215`

UI pending key 也只使用 `(ActivityToken, SubjectId)`：

- `ui/State/AppState.cs:14`
- `ui/State/AppState.cs:364-376`

Quiz VM 最終又只以 subject id 儲存 reasoning：

- `ui/State/Models.cs:229-231`

不同帳號若有相同 subject id，但題目內容或 paper contract 不同，其推理文字會被串接或錯誤歸屬。答案資料本身仍按帳號隔離，但展示給使用者的推理證據不可靠。

### P2-6：Atomic write 的提交錯誤語義可能造成記憶體／磁碟分裂

**狀態：已確認；主要影響 Unix／Android 錯誤路徑。**

`atomic_file` 先 rename 提交目的檔，再同步父目錄；若父目錄同步失敗，函式回傳 Err，但目的檔實際上已被替換：

- `core/src/atomic_file.rs:39-46`

`secrets` 等呼叫端則假設任何 Err 都代表磁碟沒有提交，因而回滾記憶體：

- `core/src/secrets.rs:202-221`

這會形成磁碟新值、記憶體舊值或錯誤的 journal 決策。

### P2-7：Config 接受無界極端值

**狀態：已確認。**

`UpdateConfig` 接受並儲存任意大的 timeout、timezone offset、iteration count：

- `core/src/engine.rs:495-553`

後續存在未檢查的乘法與加法：

- `core/src/config.rs:224`
- `core/src/monitor.rs:874`
- `core/src/monitor.rs:1434`
- `core/src/monitor.rs:1507`
- `core/src/llm.rs:244`

合法 command 因此可能造成 overflow、wrap 或 panic。這需要在 command boundary 建立可解釋、集中式的範圍驗證。

### P2-8：Providers registry 仍會靜默丟失自訂資料

**狀態：已確認。**

`Registry::load_or_seed` 在 providers JSON 讀取或解析失敗時直接退回 factory defaults，隨後以非原子 `fs::write` 寫入且忽略錯誤：

- `core/src/providers.rs:40-48`

暫時 I/O 錯誤或部分寫入可能導致使用者自訂學校／租戶設定靜默消失，也沒有 quarantine 供復原。

### P2-9：完整秘密生命週期仍未封閉

**狀態：已確認仍有殘留；完整平台驗證未完成。**

雖然 `AccountSecret` 的 Drop 會 zeroize，但其他生命週期仍包含不可主動清除的 managed／plain String：

- `Convert.ToBase64String(deviceKey)` 產生 managed string，再進入 Dictionary 與 JSON serializer：`ui/Interop/NativeCore.cs:56-59,98-102`
- Rust 只會清除自己收到的該份字串：`core/src/engine.rs:264-266`
- `get_llm_key` 會 clone plain String：`core/src/secrets.rs:230-235`
- MonitorConfig、LlmConfig、登入表單及 reqwest CookieStore 亦不在 `AccountSecret::drop` 的保護範圍。

所以不能宣稱 device key、LLM key、密碼與 session cookie 已具備完整 zeroization。

### P2-10：MockCore 與 production contract 漂移

**狀態：已確認。**

MockCore 的 `SetAnswer` 只檢查 payload 可反序列化為 `Ui.AnswerWire`，隨即發出 `AnswerUpdated`：

- `ui/Interop/MockCore.cs:120-126`

正式 Rust 還會執行非空與題型驗證：

- `core/src/monitor.rs:1529-1536`
- `core/src/protocol.rs:17-36`

Mock timeline 又會在 fixture 明知存在 conflict 時，仍無條件對帳號發出 `QuizSubmitted`：

- `ui/Interop/MockCore.cs:198-217`

因此 preview 可能顯示非法答案或未解 conflict 仍成功提交，不能作為 production 行為證據。

## 五、條件性風險與仍待實證項目

下列項目具備程式碼層風險，但是否能在真實租戶觸發，仍需錄製回應或真實端到端測試；本輪不把它們冒充成已確認事故。

### C-1：Session 驗證過寬

`verify_session` 只要求最終狀態為 2xx、path 以大小寫敏感方式不含 `login`、body 可解析成任意 JSON object：

- `core/src/login.rs:438-452`

例如 200 `{"error":"session expired"}` 或某些 SSO JSON error 可能被當成 authenticated。應驗證明確的 current-user／semester schema。

### C-2：Auth／HTTP status 分類不一致

- `fetch_classified` 只把 401 分成 AuthLost，403 會成為 Down：`core/src/monitor.rs:228-246`
- 部分 attendance／rollcall 路徑解析 JSON 前沒有一致檢查 status。
- radar probe 可能把任意 2xx 非 scope error 回應當成 InRange，之後也不一定觸發 auth recovery。

需要以真實租戶錄製的 401、403、SSO redirect、login JSON fixture 驗證。

### C-3：QR teacher assist 尚未完成真實契約

程式碼明示 request body 仍是 placeholder：

- `core/src/monitor.rs:1144-1150`

後續沒有完整檢查 HTTP status，且只接受字串型 id；數字型 id 可能被轉為空值：

- `core/src/monitor.rs:1152-1160`

此功能只能標為實驗性或未驗證，不能納入「全面完成」。

### C-4：遠端 URL 缺少 SSRF／資源上限防線

題目與課程材料可以提供任意 HTTP(S) URL：

- `core/src/answer.rs:284-300`
- `core/src/course_context.rs:114-155`
- `core/src/course_context.rs:173-190`

目前沒有明確的來源 allowlist、private-network 禁止、redirect 限制或 response size 上限。Cookie 雖受 domain 規則限制，但若內容作者不完全可信，仍存在內網探測與下載型資源耗盡風險。

### C-5：活動去重 key 可能跨 family 碰撞

poller 的 `seen` key 只使用 `{cid}/{aid}`，沒有 activity source／family：

- `core/src/monitor.rs:449-453`

若 exam、questionnaire、classroom、courseware 各表的 activity id 可重疊，先輪詢的 family 可能 suppress 後者。內部 `ActivityKey` 雖含 source，但在 poller 去重階段已可能丟失。需真租戶或錄製資料證明 ID namespace 是否全域唯一。

## 六、發版、CI 與供應鏈證據缺口

### 已確認通過

- 精確 HEAD 的 GitHub Actions 三個 job 全部成功。
- Rust unit／integration tests、clippy 與 Windows／Android builds 成功。
- Windows `ProtocolContract.Check` 與 `DeviceKey.Check` 成功。
- GitHub Actions 已使用 commit SHA pin；已核對 `actions/checkout` 與 `actions/setup-dotnet` 對應 release。
- 現存 portable ZIP 結構包含 root folder、`.portable`、UI executable 與 native DLL；marker 文件誠實說明 Data 目錄與 DPAPI 綁定。

### 仍不能據此確認

CI 沒有執行：

- 完整 `release.ps1`
- 當前 HEAD 的正式簽章 APK 與 `apksigner` 驗證
- 發版產物的 portable 啟動及資料落點 smoke test
- Android API 35 真機 timeout、process death、重啟與資料遷移
- `cargo audit`
- NuGet vulnerability scan

現存 alpha.11 APK 與 portable ZIP 是較舊產物，不能作為目前 HEAD 的可重現證據。

### 可重現性與供應鏈缺口

- CI 使用浮動的 `stable` Rust 與 `ubuntu-latest`／`windows-latest` runner。
- repo 沒有 `global.json`、`packages.lock.json` 或等價 NuGet 鎖定。
- `build-core.ps1` 與 `release.ps1` 自動選擇最新 NDK：
  - `scripts/build-core.ps1:107-115`
  - `scripts/release.ps1:226-232`
- CI pin `cargo-ndk 4.1.2`，正式 release scripts 卻沒有驗證實際 cargo-ndk 版本。
- `Cargo.lock` 已追蹤，但 CI／release 的 cargo test、clippy、build、ndk 命令沒有全面加上 `--locked`。
- MAUI package version 由已安裝 workload／SDK 注入，沒有完整 NuGet lock。

因此先前 commit 所述的 reproducible artifact gates，只能解讀為產物身分與 stale-output gate 有改善，不能解讀為完整可重現供應鏈。

## 七、文件與流程問題

### D-1：README 的 v1 repository 連結錯誤

- `README.md:6` 指向不存在的 `hot-YUser/ARTT-clean-git`。
- 正確 repository：<https://github.com/hot-YUser/auto-rollcall-thu-tronclass>

### D-2：README 的 MSIX 命令不能直接執行

- `README.md:31` 使用 `package-windows.ps1 -Mode msix`。
- 腳本要求 MSIX 必須同時指定 `-DevelopmentOnly`，文件命令會直接失敗。

### D-3：README 宣稱 production LLM key 可來自環境變數

- `README.md:48` 有此描述。
- 正式程式碼只找到 UI／vault 路徑，沒有 production environment variable source。

### D-4：Commit 語言違反專案規範

新一批 commit 的 type／scope 合法，但 description 使用英文；AGENTS 規範要求 description 與 body 使用繁體中文。這不是 runtime blocker，但不能算流程完全符合專案規範。

### D-5：審查矩陣自己仍保留未完成項

`docs/review-remediation.md` 仍明確保留：

- P1-8 Android 真機驗證待完成。
- P1-9 當前簽章 Android gate 尚未重跑。
- 完整秘密記憶體生命週期驗證待完成。
- A–E 因原始標籤與文字未保存，仍維持未驗證。

因此「所有未交付的反駁均已親自完成」在字面與證據上尚未成立。若原始 A–E 內容無法恢復，只能逐一重新審查可見的實質主張，不能捏造原文後再宣稱完成。

## 八、已確認有效的修正

第二輪審查並非否定前一輪所有成果。下列改善具有程式碼或 CI 證據：

- activity token 已成為 UI → core 命令的主要不透明路由 key，降低相同外部 activity id 跨租戶誤路由。
- core 的 per-account instance、subjects、answers 分離已有實質改善；只有 paper contract 完全相等時才共用結果。
- late participant 會獨立準備，而不是直接複用另一帳號的 attempt。
- 既有 numeric option id 的處理已修正並具測試。
- Start×2、Start→Stop 的一般 core command FSM 已改善並具測試。
- config／vault 損毀保存、quarantine 與大部分 atomic write 流程已有改善。
- Windows DeviceKey runnable check 真正通過。
- Android NotSticky、OnTimeout、service start／stop 與 backup exclusion 程式碼可以編譯。
- native stale-output、ABI、hash 與 smoke gates 比第一輪前更嚴格。

上述項目應保留，不應因第二輪發現殘留問題而回退。

## 九、建議完工計畫

### 階段 A：先封閉 P1 契約

1. 重新設計跨 activity source 的 `QuizPrepared` instance 契約。
2. 將 mutation 成功語義改為 source-specific、fail-closed。
3. 修復多帳號失敗帳號被 UI snapshot 刪除及整體完成誤判。
4. 將數字點名未知回應預設改為 Unknown／Error，加入 roster confirmation。

每一個 logical unit 都必須先有失敗測試，再修改正式程式碼，通過後獨立 Conventional Commit。

### 階段 B：封閉 FFI／UI 防線

1. Reply envelope 缺欄位、型別錯誤、null 均 fail-closed。
2. 為所有 native event family 建立 parser boundary 與 malformed fixtures。
3. 為多帳號 partial failure 建立 core-event-to-AppState integration test。
4. 為 boot failure 提供同一 process 可操作的 retry／reset 流程。
5. 將 reasoning stream key 擴充為 account-aware。
6. 讓 MockCore 與正式 core 共用或驗證相同 AnswerWire／conflict contract。

### 階段 C：持久化與秘密生命週期

1. Windows legacy migration 改成 staging + manifest／marker + atomic commit。
2. Providers 使用 atomic write、quarantine，暫時 I/O 錯誤不得覆蓋原檔。
3. 明確區分「rename 已提交但 durability sync 失敗」的錯誤狀態。
4. 在 command boundary 集中驗證 config 上下限。
5. 列出所有 secret copy 的 owner、生命週期與可／不可清除限制；不能把部分 zeroize 描述為完整清除。

### 階段 D：真實發版證據

在同一個候選 commit 上完成並保存：

1. 完整 `release.ps1` log。
2. Windows portable 實際啟動及資料落點 smoke。
3. Windows installer 安裝／解除安裝 smoke。
4. 當前 HEAD 的 signed APK。
5. `apksigner verify --verbose --print-certs` 輸出、certificate subject 與 SHA-256。
6. Android API 35 真機 start／stop／timeout／process-death／restart。
7. 舊 vault／config／device-key 遷移、故障注入及復原測試。
8. `cargo audit` 與 NuGet vulnerability scan。
9. 產物 SHA-256、toolchain／SDK／NDK／cargo-ndk 版本及 provenance。

### 階段 E：文件與矩陣收尾

1. 修正 README 的 v1 URL、MSIX 命令及不存在的環境變數 key 路徑。
2. 更新 remediation matrix；每個 finding 只能是 Confirmed、Refuted、Conditional 或 Unverified。
3. 對無法恢復原文的 A–E 明確記錄證據缺失，不得以推測補寫。
4. CI 連結更新為候選 HEAD 的精確 run。
5. Commit description／body 遵守繁體中文規範。

## 十、解除發版凍結的停止條件

只有在以下條件同時成立時，才適合重新裁定「完善」：

- 本文件四項 P1 均有修正、負向測試與正式路徑整合測試。
- 所有 activity source 通過共享跨語言契約 fixtures。
- 多帳號 partial failure 不再被隱藏或誤算完成。
- Reply／event parser 全面 fail-closed，malformed input 不會終止 UI thread。
- 候選 HEAD 通過完整 release、當前簽章、portable 與 Android 真機驗證。
- 依賴弱點掃描完成，toolchain 與 lock policy 清楚可重現。
- `docs/review-remediation.md` 不再存在未被明確接受的 release-blocking pending 項。

在此之前，正確描述應是：**「核心 hardening 與 CI 已有顯著改善，但產品契約、真實提交語義、多帳號 UI 彙總與正式發版證據仍未閉環。」**
